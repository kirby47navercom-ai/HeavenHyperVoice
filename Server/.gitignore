# CMake / Ninja build output
build/

# Rider 는 프리셋을 쓰지 않을 때 자체 빌드 디렉터리를 만든다
cmake-build-*/

# vcpkg manifest mode install tree
vcpkg_installed/

# Local development TLS certificates (never commit these)
certs/

# 비밀번호가 들어간 마이그레이션 사본. 템플릿만 커밋한다.
DataBase/002_app_user.sql

# 마이그레이션 직전 mysqldump 백업. 실제 계정 데이터가 들어 있다.
DataBase/backup/

# 파이썬 도구 캐시
__pycache__/
