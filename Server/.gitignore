# CMake / Ninja build output
build/

# Rider 는 프리셋을 쓰지 않을 때 자체 빌드 디렉터리를 만든다
cmake-build-*/

# vcpkg manifest mode install tree
vcpkg_installed/

# Local development TLS certificates (never commit these)
certs/
